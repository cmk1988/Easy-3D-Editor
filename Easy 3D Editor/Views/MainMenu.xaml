<Window x:Class="Easy_3D_Editor.Views.MainMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Easy_3D_Editor.Views"
        mc:Ignorable="d"
        WindowStyle="ToolWindow"
        Title="Easy 3D Editor" Height="130" Width="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label HorizontalAlignment="Center">Willkommen!</Label>
        <Button Grid.Row="1" Command="{Binding NewModel}">New Model</Button>
        <Button Grid.Row="2" Command="{Binding NewMap}">New Map</Button>
    </Grid>
</Window>
