<Window x:Class="Easy_3D_Editor.Views.ShapeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Easy_3D_Editor.Views"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="ShapeView" Height="210" Width="70">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Button Command="{Binding CubeCommand}">Cube</Button>
        <Button Command="{Binding SphereCommand}" Grid.Row="1">Sphere</Button>
        <Button Command="{Binding AbbortCommand}" Grid.Row="2">Abbort</Button>
    </Grid>
</Window>
