<Window x:Class="Easy_3D_Editor.ViewModels.TextureView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Easy_3D_Editor.ViewModels"
        mc:Ignorable="d"
        Title="TextureView" Height="auto" Width="auto">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Command="{Binding OkCommand}">OK</Button>
        <Button Grid.Column="1" Command="{Binding CancelCommand}">Cancel</Button>
        <Button Grid.Column="2"></Button>
        <Image Stretch="None" Source="{Binding TextureImage.Get.Source}" Grid.ColumnSpan="3" Grid.Row="1"></Image>
        <Canvas x:Name="can" Background="Transparent" Grid.ColumnSpan="3" Grid.Row="1"></Canvas>
    </Grid>
</Window>
